[DEFAULT]
debug = true

[server:main]
use = egg:PasteScript#wsgiutils
host = 0.0.0.0
port = 5000

[composite:main]
use = config:app.ini

# Add additional test specific configuration options as necessary.
